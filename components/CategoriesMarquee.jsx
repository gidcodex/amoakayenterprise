{/*import { categories } from "@/assets/assets";

export default function CategoriesMarquee() {
  const marqueeCategories = [
    ...categories,
    ...categories,
  ];

  return (
    <div className="group relative mx-auto my-3 w-full max-w-7xl select-none overflow-hidden sm:my-4">
           <div className="overflow-hidden py-3">
        <div className="flex w-max animate-marquee group-hover:[animation-play-state:paused] gap-4">
          {marqueeCategories.map((category, index) => (
            <div
              key={index}
              className="flex min-w-[170px] items-center gap-3 rounded-xl border border-gray-200 bg-white px-4 py-3 shadow-sm transition-all duration-300 hover:-translate-y-1 hover:shadow-lg"
            >
              <img
                src={category.image}
                alt={category.name}
                className="h-12 w-12 rounded-full object-cover"
              />

              <div>
                <h3 className="text-sm font-semibold text-gray-800">
                  {category.name}
                </h3>

                <p className="text-xs text-gray-500">
                  Browse Products
                </p>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  );
}*/}